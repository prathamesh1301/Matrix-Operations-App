<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.matrixoperationsapp">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.MatrixOperationsApp">
        <activity android:name=".InverseActivity"></activity>
        <activity android:name=".Matrix_Inv_Activity" />
        <activity android:name=".MultiplyActivity" />
        <activity android:name=".Matrix_Mul_Activity" />
        <activity android:name=".AddActivity" />
        <activity android:name=".Matrix_Add_Activity" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>